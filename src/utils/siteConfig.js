module.exports = {
    siteUrl: `https://blog.prudhvireddy.com/`, // Site domain. Do not include a trailing slash!

    postsPerPage: 9, // Number of posts shown on paginated pages (changes this requires sometimes to delete the cache)

    siteTitleMeta: `Prudhvi Reddy`, // This allows an alternative site title for meta data for pages.
    siteDescriptionMeta: `Developer and Life`, // This allows an alternative site description for meta data for pages.

    shareImageWidth: 1000, // Change to the width of your default share image
    shareImageHeight: 523, // Change to the height of your default share image

    shortTitle: `Life`, // Used for App manifest e.g. Mobile Home Screen
    siteIcon: `favicon.png`, // Logo in /static dir used for SEO, RSS, and App manifest
    backgroundColor: `#31B057`, // Used for Offline Manifest
    themeColor: `#31B057`, // Used for Offline Manifest
};
